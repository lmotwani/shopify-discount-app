{
  "name": "quantity-discount-app",
  "version": "1.0.0",
  "description": "Shopify app for quantity-based discounts",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "build": "concurrently \"cd backend && npm run build\" \"cd frontend && npm run build\"",
    "test": "concurrently \"cd backend && npm test\" \"cd frontend && npm test\""
  },
  "dependencies": {
    "concurrently": "^8.0.0"
  }
}
